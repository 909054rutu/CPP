#include<iostream.h>
#include<conio.h>
inline float dim(float r)
{
return (r/2);
}
inline float area(float r)
{
return (r*r);
}
inline float cir(float r)
{
return (2*3.14*r);
}
int main()
{
clrscr();
float r;
cout<<"\n enter radious";
cin>>r;
cout<<"dismtaer="<<dim(r);
cout<<"area="<<area(r);
cout<<"cir="<<cir(r);
getch();
return 0;
}